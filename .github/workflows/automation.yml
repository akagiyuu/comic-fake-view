name: automation
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch-chapter:
    runs-on: ubuntu-latest
    environment: production
    outputs:
      version: ${{ env.VERSION }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Cache Chromium
        uses: actions/cache@v3
        id: chromium-cache
        with:
          path: ~/.chromium
          key: ${{ runner.os }}-chromium-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-chromium-

      - name: Setup Chromium
        if: steps.chromium-cache.outputs.cache-hit != 'true'
        uses: browser-actions/setup-chrome@v1
        with:
          install-dependencies: true
          chrome-version: stable

      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          targets: x86_64-unknown-linux-gnu
          toolchain: nightly

      - name: Fetch
        shell: bash
        run: |
          cargo run --bin cli
        env:
          app_user_data_dir: ~/.chromium
          app_headless: true
          app_wait_for_navigation: 1
          app_max_retries: 3
          app_tab_count: 10
